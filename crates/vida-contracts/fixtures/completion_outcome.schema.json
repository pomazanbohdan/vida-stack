{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vida.local/schemas/completion-outcome-v1.json",
  "title": "CompletionOutcome",
  "description": "Golden schema fixture for the canonical completion outcome contract. The generated schema test asserts that the live schema keeps tagged alternatives and required fields.",
  "required_alternatives": [
    "passed",
    "blocked",
    "failed"
  ],
  "blocked_required_fields": [
    "blockers",
    "rework_target"
  ],
  "failed_required_fields": [
    "code",
    "retryable"
  ],
  "forbidden_wrong_variant_fields": [
    "decision",
    "verdict",
    "status"
  ]
}
