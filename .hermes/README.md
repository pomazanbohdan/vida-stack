# Hermes Host Template

This directory is the built-in framework template root for the `hermes` host CLI system.

- materialization mode: `copy_tree_only`
- runtime root default: `.hermes/`
- carrier truth remains in `vida.config.yaml -> host_environment.systems.hermes`
- this surface is a bootstrap/runtime projection, not the owner of carrier policy
