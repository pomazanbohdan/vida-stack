# Kilo Host Template

This directory is the built-in framework template root for the `kilo` host CLI system.

- materialization mode: `copy_tree_only`
- runtime root default: `.kilo/`
- carrier truth remains in `vida.config.yaml -> host_environment.systems.kilo`
- this surface is a bootstrap/runtime projection, not the owner of carrier policy
