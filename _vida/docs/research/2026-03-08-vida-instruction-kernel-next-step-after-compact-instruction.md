# VIDA Instruction Kernel Next-Step After Compact Instruction

Purpose: give the next orchestrator an exact prompt-ready instruction for the post-state-kernel artifact step: `instruction kernel spec`.

Use when: context was compacted and the next artifact to create is `_vida/docs/plans/2026-03-08-vida-0.3-instruction-kernel-spec.md`.

---

## Exact Objective

Create:

1. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-0.3-instruction-kernel-spec.md`

The artifact must define:

1. authoritative instruction entities for direct `1.0`,
2. the canonical hierarchy:
   - `Agent Definition`
   - `Instruction Contract`
   - `Prompt Template Configuration`
3. versioning, precedence, activation, and effective-instruction composition law,
4. explicit fallback, escalation, output, and proof requirements inside instruction-owned behavior,
5. the boundary between instruction semantics and provider/prompt rendering or transport topology,
6. the boundary between instruction-owned behavior and command/state/route kernels,
7. instruction-level invariants,
8. instruction-level non-goals,
9. open ambiguities,
10. downstream contracts unlocked by the spec.

---

## Required Read Order

Read these in order before acting:

1. `/home/unnamed/project/mobile-odoo/AGENTS.md`
2. `/home/unnamed/project/mobile-odoo/_vida/docs/ORCHESTRATOR-ENTRY.MD`
3. `/home/unnamed/project/mobile-odoo/_vida/docs/thinking-protocol.md`
4. `/home/unnamed/project/mobile-odoo/vida.config.yaml`
5. `/home/unnamed/project/mobile-odoo/_vida/docs/research/2026-03-08-agentic-master-index.md`
6. `/home/unnamed/project/mobile-odoo/_vida/docs/research/2026-03-08-vida-direct-1.0-next-agent-compact-instruction.md`
7. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-0.3-command-tree-spec.md`
8. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-0.3-state-kernel-schema-spec.md`
9. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-0.1-to-1.0-direct-binary-transition-plan.md`
10. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-semantic-extraction-layer-map.md`
11. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-0.2-semantic-freeze-spec.md`
12. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-0.2-bridge-policy.md`
13. `/home/unnamed/project/mobile-odoo/_vida/docs/plans/2026-03-08-vida-direct-1.0-local-spec-program.md`
14. `/home/unnamed/project/mobile-odoo/_vida/docs/research/2026-03-08-agentic-agent-definition-system.md`
15. `/home/unnamed/project/mobile-odoo/_vida/docs/agent-definition-protocol.md`
16. `/home/unnamed/project/mobile-odoo/_vida/templates/instruction-contract.yaml`
17. `/home/unnamed/project/mobile-odoo/_vida/templates/prompt-template-config.yaml`
18. `/home/unnamed/project/mobile-odoo/_vida/docs/project-overlay-protocol.md`
19. `/home/unnamed/project/mobile-odoo/_vida/docs/instruction-activation-protocol.md`
20. `/home/unnamed/project/mobile-odoo/_vida/docs/subagent-system-protocol.md`

Optional but preferred:

1. `/home/unnamed/project/mobile-odoo/_vida/docs/framework-memory-protocol.md`
2. `/home/unnamed/project/mobile-odoo/_vida/docs/silent-framework-diagnosis-protocol.md`

---

## Behavioral Inheritance Contract

The following laws MUST be carried forward into:

1. every future compact instruction,
2. every next-step instruction,
3. every child task packet,
4. every subagent prompt,
5. every worker prompt,
6. every instruction-kernel-adjacent implementation packet.

Mandatory carried behavior laws:

1. `AGENTS.md` is always the first read after compact.
2. Default lane is orchestrator unless worker-lane confirmation is explicit.
3. Undefined behavior is forbidden by default.
4. Protocols act as an allowlist, not guidance.
5. Never widen scope silently.
6. Never rely on chat memory as durable state.
7. Use subagents as the primary bounded analysis/review fabric when supported.
8. Reuse eligible subagents before falling back to local-only continuation under saturation.
9. Keep one writer/integrator ownership per shared write scope.
10. Separate authorship from verification when route law requires it.
11. No implied behavior in instructions.
12. `Instruction Contract` is the canonical logic source.
13. `Prompt Template Configuration` must not become the logic source.
14. Every prompt/packet must contain explicit fallback and escalation rules.
15. User-facing reports must be synthesized by the orchestrator, not raw subagent dumps.
16. Evidence hierarchy must prefer live/receipt/runtime evidence over chat recollection.
17. If a process gap is found, use only a bounded workaround and record the gap; do not invent a permanent path.
18. Preserve the hierarchy:
    - `Agent Definition`
    - `Instruction Contract`
    - `Prompt Template Configuration`

Propagation rule:

1. any future instruction artifact created in this program must include either:
   - a `Behavioral Inheritance Contract` section carrying these laws, or
   - an explicit reference to this file plus the general compact instruction file.

---

## Subagent Requirements For This Step

Before drafting the instruction kernel spec:

1. launch or reuse at least `2` bounded explorer lanes,
2. prefer `3` lanes because the artifact touches hierarchy, precedence, overlays, and prompt/render boundaries,
3. keep all lanes read-only,
4. keep final synthesis and writing local.

Recommended blocking questions:

1. `Explorer A`: Which current instruction-layer entities, hierarchy rules, and invariants are already frozen across the agent-definition system, protocol, and templates?
2. `Explorer B`: Which overlay, activation, precedence, and effective-composition facts must survive into the instruction kernel?
3. `Explorer C`: Which provider/rendering/prompt-transport surfaces are topology and must stay outside instruction-owned behavior, and which boundaries must remain with command/state/route kernels instead?

Each subagent prompt must carry:

1. one blocking question,
2. exact source list,
3. expected output shape,
4. no-edit restriction,
5. stop condition.

---

## Layered Working Method For This Step

Execute in this exact order:

1. restate the purpose of `instruction kernel spec`,
2. inventory current instruction-layer hierarchy and frozen laws,
3. inventory overlay, activation, precedence, and effective-composition surfaces,
4. inventory discardable provider/rendering/prompt-transport topology,
5. separate instruction-owned behavior from command/state/route/migration boundaries,
6. define entities, precedence, versioning, activation, effective composition, fallback/escalation/output/proof fields,
7. define instruction-level invariants and non-goals,
8. record open ambiguities,
9. update compact bridge so the next artifact becomes `migration kernel spec`,
10. create/update the next exact-step instruction for that following artifact,
11. report to the user in explanatory prose.

---

## Output Requirements

After writing the artifact, report to the user:

1. what was created,
2. why `instruction kernel spec` had to come immediately after the state kernel,
3. what instruction-model problem it solves in the larger `1.0` program,
4. what remains unresolved,
5. that the next exact artifact is `migration kernel spec`,
6. which subagents were used and for what bounded questions,
7. that behavioral inheritance was propagated into the next-step artifact.

---

## Hard Constraints

1. Do not reopen broad architecture debate.
2. Do not start Rust implementation.
3. Do not silently widen into external future-scope domains.
4. Do not let provider-specific prompt rendering or CLI transport become instruction law.
5. Do not let `Prompt Template Configuration` become the canonical logic source.
6. Do not let instruction semantics swallow command/state/route responsibilities that belong in other kernels.
7. Do not omit behavioral inheritance from future instructions created during this step.
