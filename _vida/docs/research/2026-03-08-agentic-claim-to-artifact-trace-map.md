# Agentic Claim-To-Artifact Trace Map

**Purpose:** Show where major research claims have already been captured, so coverage gaps can be found without rereading the entire bundle.

---

## Trace Table

| Claim ID | Claim summary | Captured in artifacts |
|---|---|---|
| C01 | more agents are not always better | atomic claims registry, source consensus/disagreement, pattern chooser, heuristics |
| C02 | single-agent remains lawful for some work | atomic claims registry, decision ledger, pattern chooser, threshold hypotheses |
| C03 | adaptive scaling beats fixed minimums when context is known | atomic claims registry, decision ledger, threshold hypotheses, master plan |
| C04 | consensus does not equal proof | atomic claims registry, research invariants, proof registry, anti-pattern catalog |
| C05 | role identity must stay separate from permissions and approval authority | atomic claims registry, decision ledger, parameter registry, master plan |
| C06 | artifact-driven handoff is safer than transcript dependence | atomic claims registry, master plan, task archetype library |
| C07 | source-sensitive tasks require refresh | atomic claims registry, invalidation watchlist, source query log, master plan |
| C08 | OWASP should be a security spine | atomic claims registry, threat/control matrix, proof registry, master plan |
| C09 | proof burden should adapt to risk and volatility | atomic claims registry, proof registry, metric glossary, heuristics |
| C10 | route rationale and traces matter | atomic claims registry, metric glossary, research implication map |
| C11 | persona/role sets should stay bounded and explicit | atomic claims registry, parameter registry, decision ledger |
| C12 | disagreement may require arbiter or reframing | atomic claims registry, disagreement matrix, heuristics, escalation matrix |
| C13 | security-sensitive work needs independent security handling | atomic claims registry, threat/control matrix, escalation matrix, proof registry |
| C14 | orchestration complexity needs evals and baselines | atomic claims registry, metric glossary, master plan |
| C15 | stable terminology is required | atomic claims registry, terminology glossary, parameter registry |

---

## Gap Rule

If a new high-salience claim does not appear in at least one:

1. knowledge layer,
2. decision/pattern/proof layer,
3. execution-facing layer,

then the bundle is not yet fully absorbed.

