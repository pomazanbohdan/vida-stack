# Multi-Agent Review Note (Slim)

Primary subagent runtime policy is defined in:

1. `_vida/docs/subagents.md`
2. `_vida/docs/subagent-prompt-templates.md`
3. `_vida/docs/DEV-AGENTS-MATRIX.md`

Rule:

1. Use multi-agent/parallel execution only when scopes are independent.
2. Consolidation and verification gates are mandatory before merge/report.
