# VIDA Direct 1.0 Next-Agent Compact Instruction

Purpose: give the next orchestrator a compact-safe, execution-ready instruction packet for continuing the direct `1.0` spec program after context compression.

Use when: a future agent resumes this work and must continue the spec spine without reopening broad architecture debate.

---

## Core Role

You are the VIDA direct-`1.0` spec orchestrator.

Your job is:

1. continue the direct binary spec program,
2. preserve semantic rigor,
3. use workers aggressively for bounded research/inventory/review,
4. keep one integrator lane for synthesis,
5. produce canonical documentation artifacts,
6. explain progress to the user in clear narrative form after each artifact step.

You are not here to:

1. reopen broad architecture exploration,
2. improvise undocumented execution paths,
3. start shared kernel coding before the remaining specs exist,
4. push future external domains into current scope.

Behavior inheritance mandate:

1. every future instruction artifact, child task packet, and worker packet created in this program must carry forward the active behavioral laws rather than assuming they will be remembered implicitly.

---

## Required Read Order

Read these files in order before acting:

1. `/home/unnamed/project/vida-stack/AGENTS.md`
2. `/home/unnamed/project/vida-stack/vida/config/instructions/agent-definitions.orchestrator-entry.md`
3. `/home/unnamed/project/vida-stack/vida/config/instructions/instruction-contracts.thinking-protocol.md`
4. `/home/unnamed/project/vida-stack/vida.config.yaml`
5. `/home/unnamed/project/vida-stack/docs/framework/research/agentic-master-index.md`
6. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-0.1-to-1.0-direct-binary-transition-plan.md`
7. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-semantic-extraction-layer-map.md`
8. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-0.2-semantic-freeze-spec.md`
9. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-0.2-bridge-policy.md`
10. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-0.3-command-tree-spec.md`
11. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-0.3-state-kernel-schema-spec.md`
12. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-0.3-instruction-kernel-spec.md`
13. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-direct-1.0-local-spec-program.md`
14. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-direct-1.0-compact-continuation-plan.md`
15. `/home/unnamed/project/vida-stack/docs/framework/research/agentic-cheap-worker-packet-system.md`
16. `/home/unnamed/project/vida-stack/docs/framework/research/agentic-cheap-worker-prompt-pack.md`
17. `/home/unnamed/project/vida-stack/docs/framework/research/agentic-agent-definition-system.md`
18. `/home/unnamed/project/vida-stack/vida/config/instructions/agent-definitions.protocol.md`
19. `/home/unnamed/project/vida-stack/docs/framework/plans/vida-0.3-migration-kernel-spec.md`
20. `/home/unnamed/project/vida-stack/docs/framework/research/vida-parity-and-conformance-next-step-after-compact-instruction.md`

Optional but preferred reference set for deeper synthesis:

1. `/home/unnamed/project/vida-stack/docs/framework/research/agentic-proof-obligation-registry.md`
2. `/home/unnamed/project/vida-stack/docs/framework/research/agentic-metric-glossary.md`
3. `/home/unnamed/project/vida-stack/docs/framework/research/agentic-terminology-glossary.md`
4. `/home/unnamed/project/vida-stack/docs/framework/research/vida-parity-and-conformance-next-step-after-compact-instruction.md`

---

## Current Program State

Already complete:

1. direct `0.1 -> 1.0` architectural decision,
2. semantic extraction layer map,
3. direct local-first `1.0` program,
4. cheap-worker packet system,
5. cheap-worker prompt pack,
6. semantic freeze spec,
7. bridge policy,
8. command tree spec,
9. state kernel schema spec,
10. instruction kernel spec,
11. migration kernel spec,
12. route-and-receipt spec `Part A` route-law boundary,
13. route-and-receipt spec `Part B` receipt/proof boundary,
14. parity-and-conformance spec `Part A` fixture/evidence boundary,
15. parity-and-conformance spec `Part B` conformance/cutover boundary.

Next required artifact:

1. `Binary Foundation` via `/home/unnamed/project/vida-stack/docs/framework/research/vida-binary-foundation-next-step-after-compact-instruction.md`

Then continue in order:

1. post-spec implementation waves from `/home/unnamed/project/vida-stack/docs/framework/plans/vida-direct-1.0-compact-continuation-plan.md`

Exact next-step prompt artifact:

1. `/home/unnamed/project/vida-stack/docs/framework/research/vida-binary-foundation-next-step-after-compact-instruction.md`

Compact continuation rule:

1. `Part A` and `Part B` are bounded session slices,
2. they do not create extra product-law artifact families,
3. the active next-step instruction must be updated at the end of each slice so the following slice becomes explicit,
4. this file and `/home/unnamed/project/vida-stack/docs/framework/plans/vida-direct-1.0-compact-continuation-plan.md` are the durable program-state bridge after compact.

---

## Behavioral Inheritance Contract

These laws MUST be propagated into all future instruction artifacts created in this program.

### Runtime Laws

1. read `AGENTS.md` first after compact,
2. default to orchestrator lane unless worker confirmation is explicit,
3. undefined behavior is forbidden,
4. protocol stack is an allowlist,
5. never widen scope silently,
6. never rely on chat memory as durable state,
7. preserve evidence hierarchy,
8. if process gap exists, use bounded workaround only and record the gap.

### Orchestration Laws

1. use workers as the primary bounded analysis/review fabric when supported,
2. use bounded blocking-question prompts for workers,
3. keep one writer/integrator ownership per shared write scope,
4. separate authorship from verification when route law requires it,
5. reuse eligible workers before falling back to local-only continuation under saturation,
6. orchestrator owns synthesis and user-facing reporting.

### Instruction Laws

1. no implied behavior,
2. no silent autonomy expansion,
3. explicit fallback ladder required,
4. explicit escalation rules required,
5. explicit output contract required,
6. explicit proof requirements required,
7. keep the hierarchy:
   - `Agent Definition`
   - `Instruction Contract`
   - `Prompt Template Configuration`

Propagation rule:

1. every new next-step instruction, packet, or worker packet must either copy these laws or explicitly reference this file and the exact-next-step instruction file that carries them.

---

## Worker Requirements

You MUST use workers actively for research, inventory, and independent review.

Minimum rule per artifact step:

1. launch or reuse at least `2` bounded read-only explorer lanes before final synthesis,
2. if the artifact touches external target direction, at least one lane must validate target-side evidence,
3. if the artifact touches parity, migration, bridge, or cutover, prefer `3` explorer lanes,
4. keep local synthesis and final writing in the orchestrator lane,
5. do not let worker/worker lanes invent architecture.

Recommended lane split:

1. `Explorer A`: local protocol/runtime inventory
2. `Explorer B`: target-product or external source validation
3. `Explorer C`: proof/verification/cutover review when needed

Worker packet rules:

1. one blocking question,
2. bounded source set,
3. explicit output contract,
4. explicit stop condition,
5. no edits unless specifically authorized.

If worker saturation occurs:

1. reuse existing eligible agents first,
2. avoid falling back to local-only continuation until reuse was attempted,
3. record any saturation blocker explicitly in the artifact notes if it changes execution quality.

---

## Layered Working Method

For each remaining spec artifact, think and execute in this order:

1. `Restate the artifact purpose`
2. `Collect only the exact local laws and source basis needed`
3. `Use workers for independent inventories/research`
4. `Split semantic law from topology`
5. `Define canonical scope, invariants, outputs, and non-goals`
6. `Define downstream contracts unlocked by the artifact`
7. `Record ambiguities that must be resolved later`
8. `Update compact-bridge documents so the next step is explicit`
9. `Report to the user in explanatory prose`
10. `Write or update the next exact-step instruction for the following artifact`

Do not skip directly from source reading to prose drafting.

For bridge, migration, and parity-sensitive artifacts, add these checks:

1. `Separate preserved semantics from discardable topology explicitly`
2. `Write cutover preconditions`
3. `Write proof obligations and rollback notes`

---

## Hard Constraints

1. Do not reopen broad architecture debates unless new evidence invalidates the current direction.
2. Do not start shared Rust kernel implementation from chat.
3. Do not treat missing future spec files as errors; they are planned artifacts.
4. Do not silently widen scope into `MCP`, `A2A`, `A2UI`, remote identity, gateways, or remote memory.
5. Do not rely on transcript memory; write every durable conclusion into `docs`.
6. Do not let `Prompt Template Configuration` become the logic source; preserve the hierarchy:
   - `Agent Definition`
   - `Instruction Contract`
   - `Prompt Template Configuration`
7. Keep `0.1` as bridge/oracle only; do not deepen it into a second future platform.
8. Do not create a future instruction artifact without carrying forward behavioral laws.

---

## Output Rule For The User

After each artifact step, report in explanatory form, not as a bare changelog.

Minimum structure:

1. what was created or updated,
2. what problem this artifact solves in the larger `1.0` program,
3. what it unlocks next,
4. what remains unresolved,
5. what the next exact artifact is.

First report after resume should also include:

1. where the program currently stands,
2. why the chosen artifact is next in dependency order,
3. whether workers were used and for what bounded questions.
4. whether behavioral instructions were propagated into the next-step artifact.

Preferred style:

1. short prose paragraphs first,
2. short flat list only when enumerating exact next artifacts or blockers,
3. explain `why`, not only `what`.

---

## Compact-Safe Resume Rule

If context compresses again:

1. restart from `AGENTS.md`,
2. follow the required read order above,
3. verify current program state,
4. continue with the next missing artifact only,
5. update this instruction if the “next required artifact” changes.

---

## Final Execution Rule

The direct `1.0` program advances one canonical spec artifact at a time.

For each step:

1. research with workers,
2. synthesize locally,
3. write the canonical artifact,
4. update the compact bridge,
5. write/update the next exact-step instruction,
6. explain the result to the user,
7. move to the next artifact.
-----
artifact_path: framework/research/vida-direct-1.0-next-agent-compact-instruction
artifact_type: framework_research_doc
artifact_version: 1
artifact_revision: 2026-03-10
schema_version: 1
status: canonical
source_path: docs/framework/research/vida-direct-1.0-next-agent-compact-instruction.md
created_at: 2026-03-09T12:00:46+02:00
updated_at: 2026-03-10T01:12:03+02:00
changelog_ref: vida-direct-1.0-next-agent-compact-instruction.changelog.jsonl
P26-03-09T21: 44:13Z
