# VIDA Mobile Docs Map

Purpose: map the extracted `vida-mobile` project documentation kept separately from the main framework surfaces.

Current surfaces:
1. `process/project-operations.md`
   - mobile project command/runbook surface extracted from framework-owned instructions

Notes:
1. Additional `vida-mobile` docs can be added here as they are discovered and separated.
2. Some references inside the extracted docs still point to their future standalone project layout and are intentionally preserved for later copy-out.
3. This bundle is not part of the default bootstrap path for the active `vida-stack` repository.
