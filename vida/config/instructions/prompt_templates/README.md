Product config home for `Prompt Template Configuration` artifacts.

Current runtime-bearing artifacts:

1. `framework_base.yaml` for minimal/shared rendering config
2. `framework_orchestrator.yaml`
3. `framework_worker.yaml`
4. `worker-packet-templates.md` for canonical worker prompt bodies
5. `cheap-worker-prompt-pack.md` for promoted script-era prompt pack

Framework-facing entrypoint:

1. `docs/framework/worker-packet-templates.md`

Format note:

1. Prompt-template configuration may remain machine-readable where runtime composition requires it.
2. Human-readable prompt bodies for `vida 0.2` and `vida v1` should live here in Markdown alongside any executable bridge artifacts.
