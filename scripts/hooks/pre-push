#!/usr/bin/env sh
set -eu

ROOT_DIR="$(git rev-parse --show-toplevel)"
if command -v pwsh >/dev/null 2>&1; then
  PS=pwsh
else
  PS=powershell
fi

exec "$PS" -NoLogo -NoProfile -ExecutionPolicy Bypass -File "$ROOT_DIR/scripts/vida-dev-gate.ps1" -Mode coverage -Json
